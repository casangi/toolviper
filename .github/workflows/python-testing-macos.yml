# .github/workflows/python-testing-macos.yml
name: "Python testing (from template)"

on:
  push:
    paths-ignore:
      - '**.md'
      - '**.rst'
  pull_request:
    paths-ignore:
      - '**.md'
      - '**.rst'

jobs:
  call-testing-linux:
    uses: casangi/toolviper/.github/workflows/python-testing-macos-template.yml@main